doctype html
html
  head
    title= title
  body
    ul
      each song in songs
        li
          form(action="/game" method="POST")
            input(type="hidden" name="song" value=song)
            button(type="submit") #{song}
